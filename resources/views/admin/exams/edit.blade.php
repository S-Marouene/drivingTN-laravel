@extends('layout')
@section('content')<div class="container py-5"><div class="d-flex justify-content-between align-items-center mb-4"><div><a href="{{ route('admin.index') }}">← Administration</a><h1 class="h3 fw-bold mt-2">{{ $exam->title }}</h1></div>@if($exam->questions()->count()<30)<a class="btn btn-primary" href="{{ route('admin.questions.create',$exam) }}"><i class="bi bi-plus"></i> Ajouter une question</a>@endif</div><div class="card mb-4"><div class="card-body"><h2 class="h5">Paramètres de l’examen</h2><form method="POST" action="{{ route('admin.exams.update',$exam) }}">@csrf @method('PUT') @include('admin.exams._form')<button class="btn btn-outline-primary">Enregistrer</button></form></div></div><div class="card"><div class="card-header bg-white d-flex justify-content-between"><strong>Questions</strong><span class="badge {{ $exam->questions()->count()===30?'text-bg-success':'text-bg-warning' }}">{{ $exam->questions()->count() }}/30</span></div><div class="list-group list-group-flush">@forelse($exam->questions as $question)<div class="list-group-item d-flex justify-content-between align-items-center"><div><span class="badge text-bg-dark me-2">{{ $question->position }}</span><span class="arabic">{{ \Illuminate\Support\Str::limit($question->question_text, 90) }}</span></div><div><a href="{{ route('admin.questions.edit',[$exam,$question]) }}" class="btn btn-sm btn-outline-primary">Modifier</a><form class="d-inline" method="POST" action="{{ route('admin.questions.destroy',[$exam,$question]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Supprimer cette question ?')">Supprimer</button></form></div></div>@empty<div class="p-4 text-muted">Ajoutez les 30 questions de cet examen.</div>@endforelse</div></div></div>@endsection
